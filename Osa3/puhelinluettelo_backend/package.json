{
  "name": "puhelinluettelo_backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:ui": "rimraf dist && cd ..\\..\\Osa3\\puhelinluettelo && npm run build && xcopy /e /i dist ..\\..\\Osa3\\puhelinluettelo_backend\\dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.2",
    "morgan": "^1.10.1",
    "rimraf": "^6.0.1"
  }
}
